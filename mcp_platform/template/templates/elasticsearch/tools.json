[
  {
    "name": "list_indices",
    "description": "List all available Elasticsearch indices",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "get_mappings",
    "description": "Get field mappings for a specific Elasticsearch index",
    "inputSchema": {
      "type": "object",
      "properties": {
        "index": {
          "type": "string",
          "description": "The Elasticsearch index name"
        }
      },
      "required": ["index"]
    }
  },
  {
    "name": "search",
    "description": "Perform an Elasticsearch search with the provided query DSL",
    "inputSchema": {
      "type": "object",
      "properties": {
        "index": {
          "type": "string",
          "description": "The Elasticsearch index to search"
        },
        "query": {
          "type": "object",
          "description": "Elasticsearch query DSL object"
        },
        "size": {
          "type": "integer",
          "description": "Number of results to return",
          "default": 10
        },
        "from": {
          "type": "integer",
          "description": "Starting offset for pagination",
          "default": 0
        }
      },
      "required": ["index", "query"]
    }
  },
  {
    "name": "esql",
    "description": "Perform an ES|QL query",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "ES|QL query string"
        },
        "format": {
          "type": "string",
          "description": "Response format (json, csv, txt)",
          "default": "json"
        }
      },
      "required": ["query"]
    }
  },
  {
    "name": "get_shards",
    "description": "Get shard information for all or specific indices",
    "inputSchema": {
      "type": "object",
      "properties": {
        "index": {
          "type": "string",
          "description": "Specific index name (optional, if not provided returns all indices)"
        }
      },
      "required": []
    }
  }
]