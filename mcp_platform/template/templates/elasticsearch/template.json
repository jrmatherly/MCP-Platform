{
  "name": "Elasticsearch",
  "description": "Official Elasticsearch MCP server implementation for connecting to Elasticsearch data",
  "version": "1.0.0",
  "author": "Data Everything",
  "category": "Database",
  "tags": ["elasticsearch", "search", "database", "analytics", "indexing", "esql", "experimental"],
  "docker_image": "dataeverything/mcp-elasticsearch",
  "docker_tag": "latest",
  "ports": {},
  "command": [],
  "transport": {
    "default": "stdio",
    "supported": ["stdio", "http"]
  },
  "capabilities": [],
  "config_schema": {
    "type": "object",
    "properties": {
      "es_url": {
        "type": "string",
        "title": "Elasticsearch URL",
        "description": "The URL of your Elasticsearch cluster (e.g., https://localhost:9200)",
        "env_mapping": "ES_URL"
      },
      "es_api_key": {
        "type": "string",
        "title": "API Key",
        "description": "Elasticsearch API key for authentication (recommended)",
        "env_mapping": "ES_API_KEY",
        "sensitive": true
      },
      "es_username": {
        "type": "string",
        "title": "Username",
        "description": "Elasticsearch username for basic authentication (alternative to API key)",
        "env_mapping": "ES_USERNAME"
      },
      "es_password": {
        "type": "string",
        "title": "Password",
        "description": "Elasticsearch password for basic authentication (alternative to API key)",
        "env_mapping": "ES_PASSWORD",
        "sensitive": true
      },
      "es_ssl_skip_verify": {
        "type": "boolean",
        "title": "Skip SSL Verification",
        "description": "Skip SSL/TLS certificate verification when connecting to Elasticsearch",
        "default": false,
        "env_mapping": "ES_SSL_SKIP_VERIFY"
      },
      "log_level": {
        "type": "string",
        "title": "Log Level",
        "description": "Logging level (DEBUG, INFO, WARNING, ERROR)",
        "default": "INFO",
        "env_mapping": "LOG_LEVEL"
      },
      "mcp_transport": {
        "type": "string",
        "title": "MCP Transport",
        "description": "MCP transport mode (stdio or http)",
        "default": "stdio",
        "env_mapping": "MCP_TRANSPORT"
      },
      "mcp_port": {
        "type": "integer",
        "title": "MCP Port",
        "description": "Port for MCP server when using HTTP transport",
        "default": 8080,
        "env_mapping": "MCP_PORT"
      }
    },
    "required": ["es_url"],
    "oneOf": [
      {
        "required": ["es_api_key"]
      },
      {
        "required": ["es_username", "es_password"]
      }
    ]
  },
  "tool_discovery": "dynamic",
  "tool_endpoint": "/tools",
  "has_image": true,
  "origin": "external",
  "experimental": true,
  "warnings": [
    "⚠️ WARNING: This MCP server is EXPERIMENTAL as noted in the official Elasticsearch repository.",
    "⚠️ This server works with Elasticsearch versions 8.x and 9.x only.",
    "⚠️ Ensure your Elasticsearch cluster is properly secured and accessible."
  ]
}
