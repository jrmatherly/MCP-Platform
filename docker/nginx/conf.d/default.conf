# Additional server configuration for MCP Platform Gateway
# This file provides additional configuration that can be customized per deployment

# Rate limiting zones (defined in main nginx.conf)
# Available zones: api, login

# Additional upstream servers can be defined here
# upstream mcp_gateway_backup {
#     server mcp-gateway-backup:8080;
# }

# Additional server blocks for specific domains
# server {
#     listen 443 ssl http2;
#     server_name api.yourdomain.com;
#     
#     include /etc/nginx/ssl_params;
#     
#     location / {
#         proxy_pass http://mcp_gateway_backend;
#         include /etc/nginx/proxy_params;
#     }
# }